language: java
jdk: openjdk8

before_script:
  - mvn clean

before_install:
  - mvn install:install-file -Dfile=botsing-reproduction/evosuite-client-botsing-1.1.1.jar -DgroupId=org.evosuite -DartifactId=evosuite-client-botsing -Dversion=1.1.1 -Dpackaging=jar

after_success:
  - mvn clean test jacoco:report coveralls:report
