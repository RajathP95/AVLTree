language: java
jdk: openjdk8

after_success:
  - mvn clean -DmemoryInMB=2000 -Dcores=2 evosuite:generate evosuite:export test jacoco:report org.eluder.coveralls:coveralls-maven-plugin:jacoco -DsourceEncoding=UTF-8
  - bash <(curl -s https://codecov.io/bash)
